machine:
  timezone: Asia/Tokyo
  node:
    version: 4
dependencies:
  pre:
    - npm install
  cache_directories:
    - node_modules
test:
  override:
    - mocha --compilers js:espower-babel/guess test/*.js
general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore
